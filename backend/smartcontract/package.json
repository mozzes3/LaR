{
  "name": "course-payment-escrow",
  "version": "1.0.0",
  "description": "Secure escrow smart contracts for course payments",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:sepolia": "hardhat run scripts/deploy-escrow.js --network sepolia",
    "deploy:ethereum": "hardhat run scripts/deploy-escrow.js --network ethereum",
    "deploy:polygon": "hardhat run scripts/deploy-escrow.js --network polygon",
    "verify": "hardhat verify"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.4"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.1",
    "dotenv": "^16.3.1"
  }
}
