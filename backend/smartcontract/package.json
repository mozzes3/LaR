{
  "name": "course-payment-escrow",
  "version": "1.0.0",
  "description": "Secure escrow smart contracts for course payments",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:registry:sepolia": "hardhat run scripts/deploy-token-registry.js --network sepolia",
    "deploy:registry:mainnet": "hardhat run scripts/deploy-token-registry.js --network mainnet",
    "add-tokens:mainnet": "hardhat run scripts/add-tokens-to-registry.js --network mainnet",
    "verify": "hardhat verify"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.4"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.1",
    "dotenv": "^16.3.1"
  }
}
